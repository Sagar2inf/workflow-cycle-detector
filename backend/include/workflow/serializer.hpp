#include <string>
#include <optional>
#include<vector>

using namespace std;

vector<string> nodeSerializer(const string& data);
pair<vector<pair<string, string>>, vector<int>> edgeSerializer(const string& data);

